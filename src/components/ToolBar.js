const ToolBar = () => {
    return (
        // TODO: buttons to adjust font size, search words, record input, night mode?
        <div>
            Tool bar
        </div>
    );
}

export default ToolBar;